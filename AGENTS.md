# AGENTS.md

## 项目定位

- 本仓库为 AI 宠物健康分析微信小程序的后端项目。
- 后端以 Python + FastAPI 为主，负责接口服务、微信登录、图片上传、AI 分析编排、缓存、限流和健康记录管理。
- 前端是微信小程序原生项目，前端依赖的稳定数据结构以 `docs/api/frontend-contract-v1.0-mvp.md` 为准。
- 代码修改应优先沿用项目已有结构和写法；当前仓库如尚未初始化代码，先按文档约定建立最小清晰结构。

---

## 技术栈

允许使用：

- Python
- FastAPI
- Pydantic
- SQLAlchemy 或项目选定 ORM
- MySQL
- Redis
- pytest

禁止使用：

- 在后端项目中引入前端框架代码
- 在前端可见位置暴露 AI API Key、微信密钥、数据库密码等敏感信息
- 让小程序前端直接调用 AI 服务
- 未经确认引入重量级新框架或无关依赖

---

## 目录约定

建议后端目录：

- `app/`：后端应用主目录。
- `app/main.py`：FastAPI 应用入口。
- `app/api/`：路由层，仅处理 HTTP 入参、鉴权、响应包装。
- `app/services/`：业务服务层，承载微信登录、上传、分析、记录等业务编排。
- `app/models/`：数据库模型。
- `app/schemas/`：Pydantic 入参、出参、内部数据结构。
- `app/repositories/`：数据库读写封装。
- `app/core/`：配置、日志、异常、依赖注入、安全工具。
- `app/utils/`：通用工具函数，如图片校验、SHA256、时间格式化。
- `tests/`：单元测试和接口测试。
- `docs/`：产品文档、接口契约、任务拆解和设计资料。

新增目录前必须确认确有必要，并保持职责清晰。

---

## 接口规则

- 后端接口必须优先满足 `docs/api/frontend-contract-v1.0-mvp.md` 中定义的前端稳定契约。
- 对外响应统一使用：

```js
{
  success: true,
  data: {},
  message: ''
}
```

失败时：

```js
{
  success: false,
  data: null,
  message: '错误提示',
  code: 'ERROR_CODE'
}
```

- 后端内部字段可以与前端字段不同，但必须在 API 出口转换为契约字段。
- API 错误码必须优先复用接口契约中的错误码。
- 图片上传、AI 分析、记录保存等接口必须有清晰的失败语义。
- AI 原始结果必须由后端清洗为结构化 `AnalysisResult`，不得把模型原始文本直接透传给前端。
- 所有医疗相关返回文案必须避免“诊断、治疗、处方”等高风险表达。

---

## 业务规则

- 微信登录由后端调用微信接口完成，前端只传 `wx.login` 得到的 `code`。
- 图片上传后端负责校验格式、大小、基础安全检查，并生成 `imageSha256`。
- AI 分析必须先检查图片 SHA256 缓存，命中缓存时直接返回历史结构化结果。
- 未命中缓存时才允许调用 AI 服务。
- 游客用户每日分析次数限制：3 次。
- 登录用户每日分析次数限制：10 次。
- 分析请求间隔限制：10 秒。
- 游客仅保留最近一次分析结果。
- 登录用户可保存并查看历史记录。
- 健康记录删除必须校验用户权限。

---

## 安全规则

- 密钥、Token、数据库连接串、微信 AppSecret、AI API Key 必须通过环境变量或安全配置读取。
- 不允许提交 `.env`、本地私有配置、临时文件或构建产物。
- 不允许在日志中记录用户敏感信息、完整访问凭证、原始密钥。
- 上传文件必须校验类型、大小和必要字段。
- 访问用户记录、删除记录、保存记录等接口必须校验用户身份或游客上下文。
- AI 调用必须在服务端完成，前端不得绕过后端访问 AI 服务。

---

## 代码规则

- 路由层保持薄：只做参数校验、依赖注入、调用 service、返回响应。
- 业务逻辑放在 `services/`，数据库读写放在 `repositories/`。
- 请求/响应结构放在 `schemas/`，避免接口字段散落在路由中。
- 公共错误处理、响应包装、配置读取、日志逻辑放在 `core/`。
- 公共工具函数放在 `utils/`。
- 命名必须清晰表达业务含义。
- 不允许大量重复代码。
- 不引入未使用依赖。
- 修改范围应聚焦当前任务，不做无关重构。

---

## AI 协作规则

- 修改代码前先查看相关路由、service、schema、repository、配置和文档。
- 涉及新增接口、接口字段、错误码或异步流程时，先同步更新 `docs/api/frontend-contract-v1.0-mvp.md`。
- 涉及功能开发时，按 `docs/development/tasks-v1.0-mvp.md` 拆解和更新任务状态。
- 涉及多文件或架构性改动时，开发前先给出简要方案并等待用户确认。
- 优先复用现有模式，不擅自引入新架构。
- 不做与当前任务无关的重构。
- 不确定业务边界时先说明假设，必要时向用户确认。
- 修改完成后说明：
  - 修改了哪些文件
  - 解决了什么问题
  - 是否影响前端契约
  - 是否需要前端配合
  - 是否已覆盖缓存、限流、鉴权和错误状态

---

## 提交前检查

- 没有新增明文密钥或敏感信息。
- API 响应符合前端契约。
- 错误码和错误提示可被前端直接使用。
- 上传文件已校验格式和大小。
- AI 调用只发生在后端。
- 图片 SHA256 缓存逻辑不重复调用 AI。
- 游客和登录用户次数限制生效。
- 记录接口已校验用户权限。
- 单元测试或接口测试覆盖核心流程。
- 项目可通过本地启动命令运行。
